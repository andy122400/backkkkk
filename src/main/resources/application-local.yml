debug: true
server:
  port: 8083

spring:
  datasource:
    username: root
    password: Accton123@
    url: jdbc:mysql://localhost:3306/agile_db

  jpa:
    hibernate:
      ddl-auto: none
    database: mysql
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true
  sql:
    init:
      mode: always


